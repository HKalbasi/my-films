# my-films

## Setup

This project uses [uv](https://github.com/astral-sh/uv) for fast Python packaging.

To install dependencies:
```bash
uv sync
```

To run the project:
```bash
uv run python build.py
```

To activate the virtual environment:
```bash
source .venv/bin/activate
```